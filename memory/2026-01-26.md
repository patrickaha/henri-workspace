# January 26, 2026 - Daily Memory

## Repoprompt SSH Setup ✅

Patrick set up direct SSH access to Repoprompt on his Mac. Key details:

**Connection:**
- SSH: `shanahanland@100.67.25.22`
- Tool: `~/RepoPrompt/repoprompt_cli`
- Access: 22 workspaces available

**How to use:**
```bash
# List workspaces
ssh shanahanland@100.67.25.22 "~/RepoPrompt/repoprompt_cli -e 'workspace list'"

# Extract a workspace
ssh shanahanland@100.67.25.22 "~/RepoPrompt/repoprompt_cli -e 'workspace extract' --workspace 'ID_HERE'"
```

**Security:** Only Patrick (U04C7A4DE) can trigger this tool.

**Key Learning:** Patrick prefers simple SSH over complex API servers. "No Express server. No tokens. No Node.js. Just SSH."

## Search Swarm Project Started

Created multi-tool search orchestrator architecture:
- Location: `02-henri/search-swarm/`
- Researched 6 tools: Perplexity, Brave, Exa, Firecrawl, Browserbase, Gemini
- Designed query-driven routing system
- Cost optimization built in

## Security Hardening

- Updated Clawdbot to 2026.1.24-3
- Fixed file permissions (600/700)
- Documented Slack token limitations (HTTP mode)
- Created Mac Mini setup guide for Patrick's friend

## X.com Bird Configuration

Bird credentials found and working:
- Config: `~/.config/bird/config.json5`
- Updated AGENTS.md with correct X.com workflow

## Key Decisions

1. Slack stays in HTTP mode (lower latency, isolated network)
2. Search Swarm uses query analysis for tool selection
3. Repoprompt access via SSH (not API)
4. Security focus on prompt injection, not infrastructure